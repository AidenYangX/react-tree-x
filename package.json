{
  "name": "react-arborist-monorepo",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "bump": "pnpm --filter react-arborist version",
    "publish": "sh bin/publish"
  },
  "private": true,
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "typescript": "^5.3.3"
  }
}

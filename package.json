{
  "name": "react-tree-x-monorepo",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "bump": "cd modules/react-tree-x && pnpm version",
    "bump:patch": "pnpm bump patch",
    "bump:minor": "pnpm bump minor",
    "bump:major": "pnpm bump major",
    "publish": "node bin/publish.js"
  },
  "private": true,
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "typescript": "^5.3.3"
  }
}

{
  "name": "react-tree-x-monorepo",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "bump": "pnpm --filter react-tree-x version",
    "publish": "node bin/publish.js"
  },
  "private": true,
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "typescript": "^5.3.3"
  }
}
